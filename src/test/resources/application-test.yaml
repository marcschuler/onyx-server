spring:
  datasource:
    url: jdbc:h2:mem:webrtc
  jpa:
    show-sql: true

logging:
  level:
    de.marcschuler: TRACE

onyx:
  init:
    startup-wizzard: false
  storage:
    path: target/test
    files: ${onyx.storage.path}/files
    previewFiles: ${onyx.storage.path}/preview
